{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    },
    "allowJs": true,           // Allows JavaScript files to be compiled by TypeScript
    "checkJs": false,          // Disables type checking for JavaScript files
    "jsx": "react",            // Specifies JSX handling (if using React)
    "noEmit": true             // Prevents emitting output files
  },
  "include": [
    "./src/**/*"              // Include all files in the src directory
  ],
  "exclude": [
    "node_modules",           // Exclude node_modules
    "**/*.test.js",           // Exclude test files if any
    "build",                  // Exclude build output directory
    "dist"                    // Exclude distribution output directory
  ]
}
